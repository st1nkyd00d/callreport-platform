# Bitácora de progreso

Registro de qué fase de `plan.md` está completa, qué falta y notas operativas para la próxima sesión/fase. Ver `CLAUDE.md` para la convención de cuándo actualizar este archivo.

---

## FASE 1 — Fundación: monorepo, base de datos, RLS y seeds

**Estado: completa** (2026-08-23)

### Qué se hizo

- Monorepo (`apps/*`, `packages/shared`) y `packages/shared` con tipos base.
- Base de datos en **Neon** (Postgres administrado) — no local, no Docker. Roles `migrator` (BYPASSRLS, dueño del schema) y `app_user` (ordinario, sujeto a RLS) creados a mano vía `apps/api/prisma/init/01-roles.sql`.
- Schema Prisma completo (`Tenant`, `User`, `TenantMembership`, `Campaign`, `CampaignAgent`, `Disposition`, `CallReport`, `AuditLog`, `PushToken`, más `Shift` agregado después de la Fase 1 original).
- 4 migraciones aplicadas contra Neon (`init`, `enable_rls`, `shifts_and_disposition_kinds`, `shifts_rls`), incluida la corrección de un bug donde las políticas RLS casteaban `current_setting(...)::uuid` contra columnas que en realidad son `TEXT`.
- Seed corrido y verificado: 2 tenants, 9 usuarios, 4 campañas, 76 turnos, 189 `call_reports`.
- **Aislamiento RLS verificado a mano con `psql` como `app_user`**: sin `app.tenant_id`/`app.role` seteados → 0 filas; seteado a Acme → solo Acme; a Globex → solo Globex; `UPDATE`/`DELETE` en `audit_logs` → `permission denied`.
- Scaffold móvil (`apps/mobile`, Expo Router, `src/app` como root): rutas placeholder `(auth)/login`, `(agent)/home`, `(client)/dashboard`, redirect inicial a `/login`. `tsc --noEmit` limpio; `expo start` (nativo y `--web`) levanta sin crashear y sirve el bundle real (HTTP 200, ~4 MB) sin errores de Metro.
- **Bug de instalación corregido**: `expo start` crasheaba siempre (`Cannot find module 'expo-router/_ctx-shared'`) por un hoisting inconsistente de npm workspaces — `@expo/cli`/`@expo/router-server` quedaban en el `node_modules` raíz pero `expo-router` solo estaba anidado en `apps/mobile/node_modules`, y ese paquete lo resuelve como si fuera hermano de `@expo/router-server`. Se agregó `expo-router` como dependencia también en el `package.json` raíz para forzar que npm lo instale ahí también.
- Se eliminó Docker del repo (`docker-compose.yml`, `apps/api/Dockerfile`) — no se usa por ahora; puede reevaluarse en Fase 8 solo para desplegar `api`/`admin-web` (la base de datos ya no depende de eso, sigue en Neon).

### Qué queda pendiente / deferido

- No se probó en un dispositivo físico ni en la app Expo Go real (solo se verificó que el servidor levanta y sirve el bundle, sin crashear, en modo headless) — recomendable una pasada manual en dispositivo antes de confiar del todo en el criterio "abre en Expo Go".
- `apps/api/src` sigue siendo el scaffold default de NestJS (`app.controller/module/service`) — no tiene `PrismaService`, auth, ni ningún módulo de negocio todavía. Eso es trabajo de Fase 2, no un pendiente de Fase 1.
- `apps/admin-web` ya tiene bastante UI construida (varias páginas de admin y de mobile-preview) pero corre sobre datos mockeados (`src/mocks/seed.ts`), no contra el API real — quedará conectado cuando Fase 3 lo requiera.

### Notas operativas

- La base es Neon: `DATABASE_URL` (endpoint directo, rol `migrator`) para migraciones/seed; `APP_DATABASE_URL` (endpoint `-pooler`, rol `app_user`, `pgbouncer=true`) para runtime. Ver `apps/api/.env.example` para el formato.
- `apps/api/prisma/init/01-roles.sql` no corre automático (no hay contenedor Docker); si se crea un proyecto/branch de Neon nuevo hay que correrlo a mano ahí primero.
- Si en el futuro `expo start` vuelve a fallar con un `MODULE_NOT_FOUND` de algún paquete de `@expo/*`, sospechar primero de hoisting roto en `node_modules` (ver el bug de arriba) antes de asumir que es un problema del código de la app.

---

## FASE 2 — Autenticación y aislamiento en el API

**Estado: completa** (2026-08-23)

### Qué se hizo

- `AuthModule` (`apps/api/src/auth/`): `POST /auth/login`, `/auth/refresh`, `/auth/logout`. Access token JWT (15 min, HS256) + refresh token opaco "selector.validador" (id de fila + secreto aleatorio hasheado con argon2 en la tabla `refresh_tokens` nueva), con rotación en cada uso y **detección de reuso**: un refresh ya rotado que se reintenta revoca toda la familia de tokens del usuario.
- `JwtAuthGuard` (global vía `APP_GUARD`, bypass con `@Public()`) + `RolesGuard` (global, lee `@Roles(...)`) + decoradores `@CurrentUser()`/`@CurrentTenant()`.
- `PrismaService.forUser(user)` (`apps/api/src/prisma/`): cliente de Prisma extendido (`$extends` + `$allOperations`) donde cada operación de modelo corre dentro de su propia transacción que primero fija `app.user_id`/`app.role`/`app.tenant_id` vía `set_config(..., true)` — el contrato de sesión que ya esperaban las políticas RLS de la Fase 1. Regla de código: los servicios de negocio solo acceden a la DB a través de este método; `AuthService` es la única excepción documentada (corre antes de que exista un usuario autenticado).
- `ValidationPipe` global (`whitelist` + `forbidNonWhitelisted` + `transform`) en `main.ts`.
- `GET /reports` (`ReportsModule`) como endpoint de humo vía `forUser()`, y `GET /admin/ping` (`AdminModule`, placeholder que reemplaza la Fase 3) protegido con `@Roles(super_admin, supervisor)` para poder probar el 403 de `RolesGuard`.
- Suite `apps/api/test/isolation.e2e-spec.ts` (7 tests, corre contra el seed real de Neon): aislamiento por tenant incluso forzando `?tenantId=` en la query, 401 sin token, 403 por rol, reuso de refresh token, y 400 por campo no declarado en el DTO.
- Login funcional en móvil: pantalla real en `(auth)/login.tsx`, sesión persistida con `expo-secure-store` (`src/lib/session.ts`), `AuthProvider`/`useAuth()` (`src/lib/auth-context.tsx`) con `authFetch()` que reintenta una vez tras un 401 refrescando el access token, redirección por rol desde `index.tsx` (`homeRouteForRole`), y botón "Cerrar sesión" en las pantallas placeholder de agente/cliente (necesario para poder probar ambos roles sin reinstalar la app).

### Qué quedó pendiente / deferido

- **No probado en Expo Go real ni en dispositivo físico** — mismo nivel de verificación que la Fase 1 (bundler headless sin errores, `tsc`/`expo lint` limpios). Falta una pasada manual: login como agente y como cliente, y confirmar que matar/reabrir la app mantiene la sesión.
- `apps/mobile` no importa tipos desde `@callreport/shared` todavía (`Role`, etc.) — se duplicó un tipo `Role` local en `src/lib/session.ts`, mismo criterio que ya usaba `apps/api/prisma/seed.ts` con `DEFAULT_DISPOSITIONS`. Cablear el import cross-workspace real (Metro necesita config adicional para paquetes del monorepo sin build propio) queda para cuando de verdad haga falta compartir más tipos con el móvil.
- `GET /admin/ping` es un placeholder solo para probar `RolesGuard`; el CRUD real de administración (tenants/usuarios/campañas) es tarea de la Fase 3, bajo el mismo prefijo `/admin`.
- El endpoint `GET /reports` es mínimo (paginación `take`/`skip` simple, sin filtros ni cursor) — la versión completa con filtros y cursor llega en la Fase 5.
- Rate limiting de `/auth/login` (fuerza bruta) es explícitamente Fase 8, no Fase 2.

### Notas operativas / bugs corregidos en el camino

- **Bug de Prisma v7 + CJS**: el generador `prisma-client` produce cliente ESM por defecto; `apps/api` es CommonJS (sin `"type": "module"`), y nada bajo `src/` había importado el cliente generado hasta esta fase, así que el problema nunca se había manifestado. `nest start` crasheaba con `ReferenceError: exports is not defined`. Fix: `moduleFormat = "cjs"` en el bloque `generator client` de `schema.prisma` + `npx prisma generate`.
- **Bug de RLS pre-existente para login de `client_user`**: la política `tenant_memberships_client_select` (Fase 1) exige `tenant_id = app.tenant_id`, pero resolver el tenant en login es precisamente el paso en el que `tenant_id` **todavía no se conoce**. Se agregó la migración `tenant_membership_self_select` con una política adicional (permissive, se combina por OR) que permite a cualquier sesión ver sus propias filas de `tenant_memberships` por `user_id`, sin depender de rol ni tenant.
- **`ts-jest` + Prisma v7 generado**: dos problemas de tooling nuevos, ambos con fix ya aplicado en `apps/api`:
  - Los imports con extensión `.js` que emite el generador de Prisma (aun en modo CJS) no resuelven bajo `ts-jest`/NodeNext sin `moduleNameMapper` — se agregó `{"^(\\.{1,2}/.*)\\.js$": "$1"}` tanto en `test/jest-e2e.json` como en el bloque `jest` de `package.json`.
  - El motor de queries "client" (WASM) de Prisma 7.9 usa `import()` dinámico, que Jest bloquea por defecto (`--experimental-vm-modules`). Se agregó `cross-env` como dependencia y el script `test:e2e` ahora corre con `NODE_OPTIONS=--experimental-vm-modules`.
- Contraseña de todos los usuarios del seed (Fase 1): `Password123!`.
- `JWT_ACCESS_SECRET` ya está en `apps/api/.env` (generado local, no commiteado); `apps/api/.env.example` documenta cómo regenerarlo.

---

## FASE 3 — Panel web de administración (CRUD)

**Estado: completa** (2026-08-26)

### Qué se hizo

- **Backend**: `TenantsModule`, `UsersModule`, `CampaignsModule` (`apps/api/src/{tenants,users,campaigns}`) con CRUD real bajo `/admin/*`, todos vía `PrismaService.forUser()`. Creación/borrado de tenants y usuarios restringido a `super_admin` (`@Roles` de método pisa al de clase); campañas/tipificaciones/asignación de agentes también editables por `supervisor`, como pide `plan.md`.
- **AuditModule** (`apps/api/src/audit/`): `@AuditEntity('X')` + `AuditInterceptor` global escribe en `audit_logs` (usuario, acción, entityId, diff superficial del body, IP) tras cada mutación exitosa, esperando el insert antes de responder (no hace falta un endpoint de lectura de auditoría para Fase 3 — eso es Fase 7).
- **`campaign_agents` ahora tiene `is_active`** (migración `campaign_agents_soft_remove`): desasignar un agente es un `UPDATE`, no un `DELETE` — ver "Notas operativas" abajo, es la misma razón por la que no hay endpoint de borrado de tipificaciones (`DELETE /admin/campaigns/:id/dispositions/:id`), solo `PATCH { isActive: false }`.
- `apps/api/src/campaigns/default-dispositions.ts` consolida el set de 8 tipificaciones por defecto (antes duplicado inline en `prisma/seed.ts`); lo usan tanto `CampaignsService.create()` como el seed.
- **Frontend**: nueva capa `apps/admin-web/src/api/` (sesión real en `localStorage`, `authFetch` con reintento-en-401, hooks TanStack Query) — mismo patrón que `apps/mobile/src/lib/`. `AdminLoginPage` + `AdminAuthGate` protegen `/admin/*`; `ClientesPage`, `UsuariosPage`, `CampanasPage`, `CampanaDetallePage` dejan de usar el `AppStore` mock y hablan con el API real.
- `test/admin-crud.e2e-spec.ts` (nuevo): flujo completo tenant → campaña (8 dispositions) → editar 3 → 2 agentes → asignación → client_user, cada mutación verificada en `audit_logs`, y 403 de supervisor creando tenant. `test/isolation.e2e-spec.ts` actualizado (`/admin/ping` ya no existe, ahora prueba con `/admin/tenants`).

### Qué quedó pendiente / deferido

- `MetricasPage`, `TurnosPage`, `AuditoriaPage` y todo `/mobile/*` (prototipos Stitch AI) siguen sobre el `AppStore` mock — están fuera de alcance de Fase 3 (Métricas es Fase 6, el visor de auditoría real es Fase 7; los mocks de `/mobile/*` son referencia de diseño para la app Expo real, no producción).
- CORS del API abierto (`app.enableCors()` sin restricción de origen) — Fase 8 tiene la tarea explícita de restringirlo al dominio real de `admin-web`.
- No se probó a mano en un navegador real con clicks (sin GUI disponible en esta sesión): se verificó en su lugar (a) la suite e2e completa contra Neon, (b) `tsc -b` + `vite build` limpios en `admin-web`, y (c) el flujo real login → `GET /admin/tenants` vía `curl` con el `Origin` del dev server de Vite, confirmando CORS y auth de punta a punta. Recomendable una pasada manual en Chrome/Firefox antes de dar Fase 3 por verificada al 100%.
- `ClientesPage` perdió la columna "Reportes este mes" (dependía del mock de `CallReport`; no hay endpoint agregado de reportes por tenant todavía — llega con Fase 5/6).

### Notas operativas

- **`app_user` nunca tiene `GRANT DELETE`** en ninguna tabla (ver `prisma/init/01-roles.sql`: "la aplicación nunca borra filas físicamente, usa flags de estado en su lugar") — esto rompió el diseño inicial de `PUT /admin/campaigns/:id/agents` (usaba `deleteMany`) y de un endpoint `DELETE` de tipificaciones que se había planeado. Ambos se rediseñaron sin ningún `DELETE`/`.delete()`/`.deleteMany()`: asignar agentes es upsert + `updateMany({isActive:false})`, "borrar" una tipificación es `PATCH {isActive:false}`. Si una fase futura necesita un hard-delete real de algo, hay que revisar primero si `01-roles.sql` debe cambiar (impacto amplio, pensarlo dos veces).
- `forUser()` corre cada operación en su propia transacción (no se puede meter un create de dos modelos relacionados en un único `$transaction` sin saltarse el contexto RLS) — por eso `UsersService.create()` y `CampaignsService.create()` **validan la existencia del tenant ANTES** de crear la fila principal, en vez de crear-y-compensar-con-delete-si-falla (que tampoco sería posible sin `GRANT DELETE`).
- Contraseña inicial de usuarios creados desde `UsuariosPage` la define quien los crea (campo nuevo en el formulario) — no hay generación automática ni email de bienvenida (fuera de alcance).
- Sesión de `admin-web` en `localStorage` (`callreport.admin.session`), separada de la sesión mock de `AppStore`/`RoleSwitcher`.

---

## FASE 4 — Flujo del agente (móvil)

**Estado: completa** (2026-08-26)

### Qué se hizo

- **Migración RLS nueva** (`prisma/migrations/20260826040000_agent_report_edit_rls`): dos huecos que quedaron abiertos en fases anteriores y bloqueaban/exponían el flujo del agente — (a) no existía política de `UPDATE` para `agent` en `call_reports` (con `FORCE ROW LEVEL SECURITY`, un `PATCH` del propio autor afectaba 0 filas aunque el servicio ya hubiera validado la ventana), se agregó `call_reports_agent_update`; (b) `campaign_agents.is_active` (Fase 3) no lo miraba ninguna política todavía — un agente desasignado seguía viendo e insertando en esa campaña —, se agregó `AND is_active` a las cuatro políticas de agente que consultan `campaign_agents`. Verificado a mano contra Neon con un script Node/`pg` (sin `psql` disponible en esta sesión): UPDATE de reporte propio afecta 1 fila, de reporte ajeno afecta 0; con la asignación en `is_active=false`, `SELECT` sobre la campaña devuelve 0 filas y vuelve a 1 al reactivarla.
- **`ShiftsModule`** (`apps/api/src/shifts/`, nuevo — no estaba en el alcance original de la fase, ver nota en `plan.md`): `GET /agent/shifts/current`, `GET /agent/shifts?days=`, `POST /agent/shifts/clock-in`, `POST /agent/shifts/clock-out`. Clock-out nunca borra, solo sella `endedAt` (mismo criterio que el resto del backend: sin `GRANT DELETE`).
- **`CampaignsModule`** suma `AgentCampaignsController` (`GET /agent/campaigns`, campañas activas asignadas) y `DispositionsController` (`GET /campaigns/:id/dispositions`, sin `@Roles` — RLS ya decide qué campaña puede ver cada rol; lo reutilizará el dashboard del cliente en Fase 5).
- **`ReportsModule`** suma `POST /reports` y `PATCH /reports/:id` (`ReportsController`) y `GET /agent/reports?range=today|week` (`AgentReportsController`, nuevo). `tenant_id`/`agent_id`/`shift_id` se derivan en el servidor; ninguno de los tres existe en los DTOs, así que el `ValidationPipe` global (`forbidNonWhitelisted`) los rechaza con 400 si alguien los manda. `campaign_agents` no tiene RLS (a diferencia de `campaigns`/`dispositions`/`call_reports`) — `ReportsService.create()` la consulta primero, antes de tocar `campaigns` vía `forUser()`, precisamente para poder distinguir "no asignado" (403) de "no existe" (404); si el orden se invirtiera, RLS ya habría filtrado ambos casos a lo mismo.
- **Sanitización** (`apps/api/src/common/sanitize.ts`): `sanitizeNotes` (trim + strip HTML + tope de longitud vía `@MaxLength`) y `normalizePhone`, aplicados con `@Transform` de `class-transformer` en los DTOs de reporte (antes de que corran los validadores, no en el servicio).
- **Tests**: `apps/api/test/agent-reports.e2e-spec.ts` (nuevo) cubre turno → crear reporte (tenant/shift derivados, auditado) → campaña no asignada (403) → tipificación cruzada (400) → `tenantId` inyectado (400) → sin turno (409) → edición dentro de ventana (200) → `createdAt` retrocedido 31 min (autor 403, supervisor 200, ambos auditados). Las 4 suites e2e (10 tests) pasan.
- **Móvil**: `@tanstack/react-query` y `@react-native-async-storage/async-storage` agregados vía `npx expo install` (sin datetimepicker ni NetInfo — decisiones tomadas al planificar la fase). El placeholder `(agent)/home.tsx` de la Fase 1 se reemplazó por un grupo de tabs (`(agent)/_layout.tsx`: Reportar / Mis reportes / Turno, más `seleccionar-campana` y `editar-reporte/[id]` ocultas de la barra con `href: null`). Capa de datos en `src/lib/` (`agent-types.ts`, `agent-queries.ts` con los hooks de React Query, `selected-campaign.ts`, `report-queue.ts`, `api-json.ts` espejo de `apps/admin-web/src/api/client.ts`). Componentes compartidos `report-form.tsx` (crear y editar) y `disposition-chip.tsx`.
- **Cola offline** (`report-queue.ts`, AsyncStorage): un fallo de *red* al crear un reporte lo encola; un rechazo real del servidor (400/403/409) se muestra al agente y no se reencola. El reintento automático corre en `(agent)/_layout.tsx` vía `AppState` (al entrar a la sección y al volver a primer plano) — no hay NetInfo instalado, es la señal de "puede que volvió la conexión" más simple disponible sin sumar otra dependencia nativa.

### Qué quedó pendiente / deferido

- **No probado en Expo Go real ni en dispositivo físico** — mismo nivel de verificación que las fases anteriores: `tsc --noEmit` y `expo lint` limpios, y `expo start` (nativo vía `/index.bundle` — devolvió 404 en la ruta que probé, pero el server no crasheó — y `--web`, HTTP 200, bundle completo de ~943 módulos sin errores) headless. Faltan los dos criterios que solo se pueden verificar a mano: el flujo completo en <30 s en un dispositivo real, y el comportamiento real en modo avión (la cola y el reintento están implementados y compilan, pero nunca se probó con conectividad real intermitente).
- La cuenta regresiva de "N min restantes" en "Mis reportes" usa un default de 30 min hardcodeado en el móvil (`DEFAULT_EDIT_WINDOW_MINUTES`, `agent-types.ts`) — ningún endpoint expone hoy `tenant.editWindowMinutes` a un agente (no tiene `tenantId`, no es staff). Es solo una aproximación de UI: el servidor sigue siendo la única fuente de verdad real (el `PATCH` devuelve 403 con el mensaje correcto si la ventana configurada por tenant ya venció, aunque la UI todavía muestre minutos positivos). Si algún tenant cambia `editWindowMinutes` del default, revisar si vale la pena exponerlo.
- Clock-out manual de supervisor sobre un turno ajeno (`Shift.closedBy`, ya existe en el schema desde antes de esta fase) no tiene endpoint ni UI todavía — es trabajo de admin-web, no de esta fase.
- El campo de cita (`scheduledAt`, tipificación "Cita Agendada") es un `TextInput` de texto libre en formato `AAAA-MM-DD HH:mm`, sin datepicker nativo (decisión tomada al planificar la fase, para no sumar otro módulo nativo al monorepo).

### Notas operativas

- **`apps/api/package.json`**: `test:e2e` ahora corre con `--runInBand`. Con 4 suites e2e, cada una levantando su propio `Nest`/`PrismaClient` contra el endpoint *pooled* de Neon, correrlas en paralelo (comportamiento default de Jest) agota las conexiones disponibles y varias fallan con `Authentication timed out` / timeouts de Jest — intermitente, no un bug del código. En serie (`--runInBand`) las 4 suites (10 tests) pasan de forma consistente.
- Los turnos (`Shift`) no tienen `tenantId` — son un concepto laboral interno del call center, no de una campaña/empresa cliente. `ShiftsController` no necesita saber el tenant del agente para nada.
- Si una fase futura agrega otra tabla sin RLS parecida a `campaign_agents` (Fase 3), tener presente el patrón usado acá: una tabla sin `FORCE ROW LEVEL SECURITY` es la única forma de distinguir "no tenés permiso" de "no existe" cuando la tabla *con* RLS relacionada ya filtra ambos casos al mismo resultado vacío.

---

## FASE 5 — Tiempo real: dashboard del cliente (móvil)

**Estado: completa** (2026-08-27)

### Qué se hizo

- **`RealtimeModule`** (`apps/api/src/realtime/`, nuevo): `RealtimeGateway` con Socket.io en `/ws`. El handshake exige `auth.token` (mismo JWT y secreto que `AuthService`, verificado con `JwtService.verifyAsync`); sin token o inválido, `client.disconnect(true)`. El room se deriva **solo** del payload verificado en el servidor: `client_user` → `tenant:{tenantId}`, `supervisor`/`super_admin` → `staff`, `agent` → sin room (no hay evento dirigido a agentes en esta fase). `emitReportCreated`/`emitReportUpdated` emiten a `[tenant:{tenantId}, 'staff']`.
- **`ReportsService`** (`apps/api/src/reports/`): `create()`/`update()` ahora emiten por el gateway después de confirmar el write. `GET /reports` pasó de ser el endpoint de humo de la Fase 2 (`take`/`skip`, devolvía un array) a la versión completa: filtros `from`/`to`/`campaignId`/`dispositionId` (CSV)/`scheduledFrom`, paginación por cursor (`{ items, nextCursor }`). Nuevo `GET /reports/summary` (agregado con `groupBy`, no `$queryRaw` — ver gotcha abajo) y `GET /reports/:id`. **Cambio de contrato**: `isolation.e2e-spec.ts` (Fase 2) se actualizó para leer `res.body.items` en vez de `res.body` directo.
- **`ClientCampaignsController`** (`apps/api/src/campaigns/`, nuevo): `GET /client/campaigns` (incluye `tenant.name`, lo usa el header del dashboard) y `GET /client/dispositions` — espejo de `AgentCampaignsController`, RLS ya limita ambos al tenant del JWT.
- **Orden de `GET /reports` depende del filtro**: con `scheduledFrom` (carrusel de "Próximas citas") ordena por `scheduledAt asc` (la cita más próxima primero); sin él, `createdAt desc` como el feed normal. Verificado a mano con `curl` que las fechas salen en orden ascendente.
- **Test e2e** (`apps/api/test/realtime-reports.e2e-spec.ts`, nuevo, 8 tests): auth del gateway (sin token / token inválido → desconectado), aislamiento por tenant en tiempo real (un reporte nuevo llega solo al room de su propio tenant — el test no asume de antemano si el agente de prueba está asignado a una campaña de Acme o Globex, porque el seed cruza asignaciones al azar; determina el tenant dueño después de crear el reporte), filtros `from`/`to`/`dispositionId` contrastados contra `prisma.forUser(user).callReport.count()`, `GET /reports/summary` (suma de `byDisposition` == `total` == conteo real), y `GET /reports/:id` (detalle propio 200, de otro tenant 404). Las 5 suites e2e (18 tests) pasan en conjunto.
- **Móvil**: `RealtimeProvider` (`src/lib/realtime.tsx`) crea el socket con `auth: { token }` cuando hay sesión de `client_user`; en `connect_error` intenta refrescar el access token una vez (`refreshAccessToken`, extraído de `authFetch` en `auth-context.tsx`) y reconecta. El socket es **mejora de experiencia, no fuente de verdad** (riesgo ya documentado en `plan.md`): tanto los eventos (`report.created`/`report.updated`) como la reconexión y `AppState → 'active'` simplemente invalidan las queries de React Query (`client-reports`, `reports-summary`, `upcoming-appointments`) en vez de mutar el caché a mano — el REST decide qué se muestra. La animación de entrada de un ítem nuevo (`Animated.View entering={FadeInDown}` en `report-card.tsx`) sale gratis del primer montaje del ítem tras el refetch, sin necesidad de un `setQueryData` quirúrgico.
- **Dashboard del cliente** (`(client)/dashboard.tsx`, reemplaza el placeholder de la Fase 1): header con nombre del tenant + badge "En vivo"/"Sin conexión" + logout; chips de rango (Hoy/Semana/Mes/Personalizado); chips de tipificación agrupados por `code` (mismo criterio que el mock de Stitch — dos campañas con la misma tipificación por defecto comparten chip, filtran por la lista de ids agrupados); tarjetas KPI (Total/Ventas/Citas/Seguimientos/Otros, agregadas del lado del cliente desde `GET /reports/summary`); carrusel "Próximas citas" (decisión tomada al planificar la fase, fuera del `plan.md` original); feed con `FlatList` + `onEndReached` → `fetchNextPage`. Nueva pantalla `(client)/reporte/[id].tsx` para el detalle.
- **Selector de rango personalizado sin dependencia nueva**: en vez de instalar `@react-native-community/datetimepicker` (la decisión tomada al planificar la fase), se usó `@expo/ui/community/datetime-picker` — ya era dependencia de `apps/mobile` (`@expo/ui`) y expone un drop-in compatible con la misma API, con Jetpack Compose/SwiftUI nativos. Cero paquetes nuevos para esto.
- `apps/mobile/src/lib/auth-context.tsx`: `login`/`logout`/`authFetch`/`refreshAccessToken` se envolvieron en `useCallback` (antes eran funciones planas redefinidas en cada render) — necesario porque `realtime.tsx` usa `refreshAccessToken` como dependencia de un `useEffect`; sin identidad estable, el socket se reconectaría en cada render de `AuthProvider`.

### Qué quedó pendiente / deferido

- **No probado en Expo Go real ni en dispositivo físico** — mismo nivel de verificación que todas las fases anteriores: `tsc --noEmit` y `expo lint` limpios, `expo start --web` bundlea sin errores (1315 módulos), `expo start` nativo levanta sin crashear (el `/index.bundle` directo devuelve 404, mismo comportamiento ya documentado en la Fase 4 — no es una regresión). Faltan las pruebas manuales con dos sesiones reales: latencia real del socket en un dispositivo, resync real tras background, y el flujo completo de filtros tocando la UI.
- El feed principal (`GET /reports` sin `scheduledFrom`) sigue ordenado por `createdAt desc` con cursor por `id` — no soporta cursor combinado con el orden `scheduledAt asc` que usa "Próximas citas"; no hace falta porque ese carrusel pide una sola página de 10 sin paginar, pero si una fase futura necesita paginar citas futuras, hay que generalizar el cursor.
- El feed en tiempo real actualiza vía `invalidateQueries` (refetch completo), no vía inserción quirúrgica en el caché (`setQueryData`) como sugería el plan original — decisión tomada durante la implementación: soportar la inserción exacta requeriría reevaluar los filtros activos del lado del cliente para cada evento entrante (rango de fecha + tipificación agrupada por code), duplicando lógica que el servidor ya resuelve correctamente. `invalidateQueries` es más simple y más correcto (siempre refleja lo que el servidor decidiría), a costa de una llamada REST extra por evento — aceptable al volumen de este proyecto (<20 agentes).

### Notas operativas

- **Gotcha para la Fase 6**: `plan.md` dice explícitamente "agregados con SQL crudo (`GROUP BY` + `date_trunc`)" para `GET /admin/metrics/*`. Eso **no funciona tal cual** con `PrismaService.forUser()`: `forUser()` solo fija los GUC de sesión (`app.user_id`/`app.role`/`app.tenant_id`) para operaciones que tienen `model` (ver el `if (!model)` en `prisma.service.ts`); un `$queryRaw` suelto cae en esa rama sin contexto de tenant y devolvería filas de todos los tenants (o ninguna, según la política). `GET /reports/summary` de esta fase se resolvió con `groupBy` (sí pasa por el interceptor) en vez de raw SQL. Si Fase 6 de verdad necesita `date_trunc`/agregados más complejos que `groupBy` no cubre, hay que agregar un método tipo `forUserRaw()` que abra su propia transacción y corra el `set_config` a mano antes del raw query — no asumir que `forUser()` ya lo cubre.
- El gateway de Socket.io tiene su propia config de CORS (`cors: { origin: true }` en el decorador `@WebSocketGateway`), independiente de `app.enableCors()` en `main.ts`. Si Fase 8 restringe el CORS del API a un dominio fijo, hay que actualizar también el del gateway o el handshake desde el navegador (`expo start --web`) empieza a fallar.
- El test e2e de sockets (`realtime-reports.e2e-spec.ts`) es el único que llama `app.listen(0)` en vez de dejar que `supertest` levante un server efímero — `socket.io-client` necesita una URL real para conectar. Sigue corriendo con `--runInBand` (ya configurado en `test:e2e`), y el `afterAll` desconecta todos los sockets abiertos antes de `app.close()` para no colgar Jest.
- Bug de timing descubierto escribiendo el test de aislamiento por socket: el servidor emite el evento **antes** de que la respuesta HTTP del `POST /reports` vuelva al test (mismo request handler). Un `waitFor()` que registra su listener `.once()` recién *después* de esperar esa respuesta llega tarde -- el evento ya se disparó y no hay una segunda emisión. Los listeners de eventos de socket que dependen de una acción disparada por el propio test tienen que registrarse **antes** de disparar esa acción, no después.

---

## FASE 6 — Push, cola de seguimientos y métricas de agentes

**Estado: completa** (2026-08-27)

### Qué se hizo

- **Migración `push_tokens_and_followup_rls`**: `PushToken.revokedAt` (baja lógica -- `app_user` nunca tiene `GRANT DELETE`, mismo criterio que `RefreshToken.revokedAt`/`CampaignAgent.isActive`) + `push_tokens` pasa a tener RLS (no la tenía desde la Fase 1: cualquier rol podía leer los tokens de cualquier usuario; se cierra recién ahora que existe un consumidor real). Dos huecos nuevos descubiertos al planificar la fase (ver `plan-fase-6.md`, decisiones D1/D2): `call_reports_client_update` (no existía ninguna política de UPDATE para `client_user`, mismo hueco que ya había aparecido para `agent` en la Fase 4) + un **trigger** `enforce_client_followup_only` que impide que un `client_user` toque cualquier columna de `call_reports` que no sea `followup_resolved_at`/`_by` -- una política RLS es todo-o-nada por fila, no puede limitar columnas. Verificado a mano contra Neon con un script Node/`pg` (6 pruebas: resolver propio tenant, resolver otro tenant, trigger bloqueando otra columna, aislamiento de `push_tokens` por usuario, "sin settings → cero filas").
- **`PrismaService`** suma dos métodos nuevos (`forUser()` se refactorizó a un `withContext()` privado compartido): `forSystem()` (contexto `app.role='super_admin'` sin usuario HTTP real, para que `NotificationsService` pueda resolver `tenant_memberships`/`push_tokens` de otros usuarios al hacer targeting de push -- único consumidor legítimo, documentado en el código) y `forUserRaw()` (abre la transacción y fija los GUC a mano antes de pasarle el `Prisma.TransactionClient` a un callback, para que `$queryRaw` corra con contexto RLS -- el gotcha que ya había quedado anotado en la bitácora de la Fase 5).
- **`NotificationsModule`** (`apps/api/src/notifications/`): `PushService` (wrapper de `expo-server-sdk`, envío en lotes + `getReceiptsAsync`), `NotificationsService` (targeting: `client_user` del tenant siempre, supervisores solo si `disposition.requiresFollowup`, vía `forSystem()`; maneja tickets y programa un chequeo de receipts ~15 min después con `setTimeout().unref()`, sin tabla ni cron dedicados a esta escala), `PushTokensController` (`POST`/`DELETE /push/register`). `ReportsService.create()` llama a `notifyReportCreated()` **fire-and-forget** después de emitir el socket -- una caída de Expo nunca puede tumbar un 201 ya confirmado en la base.
- **`FollowupsModule`** (`apps/api/src/followups/`): `GET /followups?status=pending|resolved` (cursor, mismo criterio que `GET /reports`; pendientes ordenados por `scheduledAt` más próximo primero, resueltos por cuándo se resolvieron), `GET /followups/count` (badge), `POST /followups/:reportId/resolve` (`client_user` del tenant o staff; 400 si la tipificación no requiere seguimiento, 409 si ya estaba resuelto -- idempotencia explícita). Nuevo decorador `@AuditAction()` (`audit-entity.decorator.ts`) para que el interceptor registre `action='resolve_followup'` en vez del `update` que le tocaría por ser un POST.
- **`MetricsModule`** (`apps/api/src/metrics/`, `@Roles(supervisor, super_admin)`): `GET /admin/metrics/agents` (por agente: total, horas activas recortadas al rango vía `shifts`, promedio por hora activa, distribución por tipificación con `requiresFollowup` incluido -- lo termina usando también la tabla de `admin-web`) y `GET /admin/metrics/overview` (`byDay`/`byTenant`/`byCampaign` + KPIs `totalReports`/`activeTenants`/`pendingFollowups`/`agentsOnShift`), todo vía `forUserRaw()` con SQL crudo (`GROUP BY`/`date_trunc`, como pide `plan.md`). `tz` configurable (`METRICS_TZ` en `.env`, default `America/Argentina/Buenos_Aires`) para que "volumen por día" agrupe en el huso horario del call center, no en UTC.
- **`RealtimeGateway`** suma `emitFollowupResolved()` (rooms `tenant:{tenantId}` + `staff`, mismo patrón que `report.created`/`report.updated`).
- **Móvil**: `expo-notifications` instalado (plugin agregado a `app.json`, `extra.eas.projectId` placeholder vacío -- ver pendientes). `src/lib/push.ts` (registro/baja del token, canal Android antes de pedir el token en Android 13+, `setNotificationHandler` para foreground), `use-push-registration.ts` (montado en `(agent)/_layout.tsx` y `(client)/_layout.tsx`, después del login, nunca en el arranque), `use-notification-deep-link.ts` (montado en el `_layout` raíz dentro de `AuthProvider`; cubre foreground/background con `addNotificationResponseReceivedListener` y arranque en frío con `useLastNotificationResponse()`, deduplicando por `notification.request.identifier` para no navegar dos veces). `(client)/_layout.tsx` pasa de `Stack` a `Tabs` (Inicio/Seguimientos) con `tabBarBadge` de pendientes; nueva pantalla `(client)/seguimientos.tsx` (Pendientes/Resueltos, botón "Marcar como resuelto" -- no swipe, decisión tomada al planificar la fase --, resaltado de vencidos). `realtime.tsx` invalida las queries de seguimientos también en `report.created`/`report.updated`/`followup.resolved`. `auth-context.tsx#logout()` da de baja el token de push ANTES de limpiar la sesión local (agujero de aislamiento no listado en `plan.md`: sin esto, un dispositivo que pasa de un `client_user` de un tenant a otro de otro tenant seguiría notificando al tenant anterior).
- **Admin-web**: `src/api/metrics.ts` (hooks TanStack Query) + `MetricasPage.tsx` reescrita sobre datos reales (se conservó toda la UI de Recharts) con selector de rango (7/30/90 días + personalizado). La dona de "Distribución de tipificaciones" no tiene endpoint propio (`plan.md` solo pide `byDay`/`byTenant`/`byCampaign` para overview) -- se deriva sumando `byDisposition` entre los agentes activos de `/admin/metrics/agents`, documentado en el código como simplificación aceptada.
- **Tests**: 3 suites e2e nuevas -- `push-notifications.e2e-spec.ts` (7 tests: registro/upsert/reasignación de dueño, baja lógica, formato inválido, aislamiento por usuario, targeting con `PushService` sustituido por un doble, receipt `DeviceNotRegistered`), `followups.e2e-spec.ts` (5 tests: flujo completo con socket, 404 entre tenants, 400/409, trigger de columnas), `metrics.e2e-spec.ts` (6 tests: 403 por rol, tz inválido, cada gráfico contrastado contra una consulta de control vía Prisma). Las 8 suites (36 tests) pasan juntas con `--runInBand`.

### Qué quedó pendiente / deferido

- **Push real nunca se probó en un dispositivo físico** (bloqueador de infraestructura, no de código -- ver decisión D5 en `plan-fase-6.md`): `getExpoPushTokenAsync()` exige un `projectId` de EAS real y `app.json` hoy tiene `extra.eas.projectId: ""`; hace falta correr `npx eas init` una vez con la cuenta de Expo del proyecto. Además, desde el SDK 53 Expo Go ya no entrega push remoto en Android -- hace falta un *development build* (`eas build --profile development`), tarea que ya estaba prevista para la Fase 8. Mientras tanto: targeting, registro/baja de tokens y receipts están cubiertos con tests e2e reales contra Neon; el deep link se probó con una notificación **local** (`Notifications.scheduleNotificationAsync`, no requiere push remoto) en Expo Go.
- Receipts de Expo se consultan con un `setTimeout` en memoria (`NotificationsService`), no con una tabla + cron -- si el proceso reinicia entre el envío y el chequeo (~15 min después), ese chequeo puntual se pierde. Un token realmente inválido vuelve a fallar en el siguiente envío y se da de baja ahí; aceptado como límite a esta escala (<20 agentes), igual que las tablas de resumen de métricas que ya quedaron anotadas como optimización futura en `plan.md`.
- `TurnosPage` y `AuditoriaPage` (`admin-web`) siguen sobre el `AppStore` mock -- fuera de alcance (`TurnosPage` necesita el clock-out de supervisor sobre turno ajeno, que no tiene endpoint todavía; el visor de auditoría real es Fase 7).
- No se probó a mano en un navegador real con clicks el selector de rango de `MetricasPage` ni la pantalla `Seguimientos` del móvil en Expo Go/dispositivo físico -- mismo nivel de verificación que el resto de fases: `tsc`/`vite build`/`oxlint` limpios en `admin-web`, `tsc --noEmit`/`expo lint` limpios y `expo start` (nativo y `--web`) bundlea sin errores en `apps/mobile`.
- CORS del API y del gateway de sockets siguen abiertos (tarea explícita de Fase 8, ya anotada en la bitácora de la Fase 5).

### Notas operativas

- **`expo-server-sdk` (v7) es ESM puro y rompe a Jest en este entorno** (Node 22; el `require(ESM)` síncrono que Jest necesita para su loader recién llega en Node 24.9): un `import { Expo } from 'expo-server-sdk'` estático en cualquier archivo alcanzable desde `AppModule` (que TODAS las suites e2e cargan) hacía fallar la carga del módulo con `Must use import to load ES Module`. Se resolvió con **import() dinámico y perezoso** dentro de `PushService` (mismo patrón que ya usa el motor WASM de Prisma en este proyecto, de ahí que `test:e2e` ya corriera con `NODE_OPTIONS=--experimental-vm-modules`) y con un helper local `isExpoPushToken()` (`push-token-format.ts`, réplica exacta de `Expo.isExpoPushToken`) para que el DTO de validación (`register-push-token.dto.ts`, que SÍ corre en el hot path de cada request) no dependa del SDK en absoluto. Si en el futuro se actualiza el SDK y vuelve a ofrecer un build CJS, se podría volver a un `import` estático, pero no hay apuro.
- **Gotcha de zonas horarias**: `Intl.supportedValuesOf('timeZone')` (Node) solo lista IDs *canónicos* (p.ej. `America/Buenos_Aires`) y no reconoce alias válidos como `America/Argentina/Buenos_Aires` que Postgres sí acepta en `AT TIME ZONE` -- validar con esa lista rechazaba el propio default de `METRICS_TZ`. Se cambió a validar construyendo `new Intl.DateTimeFormat('en', { timeZone })` dentro de un `try/catch` (sí resuelve alias).
- **Regla de código nueva**: el único consumidor legítimo de `PrismaService.forSystem()` es `NotificationsService` -- es un contexto con privilegios de staff sin usuario HTTP real detrás, no un atajo para saltarse RLS en otro lado. El grep de CI que Fase 8 tiene planeado para validar el uso exclusivo de `forUser()` tiene que extenderse para cubrir también `forSystem()`/`forUserRaw()`.
- Contraseña de todos los usuarios del seed sigue siendo `Password123!` (sin cambios desde la Fase 1).

---

## FASE 7 — Exportación (CSV/PDF) y visor de auditoría

**Estado: completa** (2026-08-27)

### Qué se hizo

- **Migración `audit_logs_rls`**: `audit_logs` quedó fuera de RLS desde la Fase 1 ("no es multi-tenant en sí misma"), pero sí es cross-tenant -- contiene acciones/IPs/diffs de todos los tenants. El visor de esta fase la lee, así que se cierra con `ENABLE`/`FORCE ROW LEVEL SECURITY` + `audit_logs_staff_select` (SELECT solo supervisor/super_admin) + `audit_logs_self_insert` (INSERT solo con `user_id = app.user_id`, imprescindible porque `AuditInterceptor` escribe para *cualquier* rol). Verificado a mano contra Neon con un script Node/`pg` (6 pruebas, mismo patrón que Fases 4/6): aislamiento por rol, self-insert, y el REVOKE de UPDATE/DELETE re-verificado.
- **Gotcha real encontrado al encender esa RLS**: `Prisma.create()` hace `INSERT ... RETURNING` internamente, y Postgres exige que la fila devuelta por un `RETURNING` pase también la política de **SELECT** -- no solo el `WITH CHECK` del INSERT. Como `audit_logs_staff_select` solo deja leer a staff, un agente o `client_user` insertando su propia fila de auditoría hacía fallar el `POST /reports`/`POST /followups/.../resolve` entero con "new row violates row-level security policy", aunque el INSERT en sí fuera válido. Se resolvió reescribiendo `AuditInterceptor.write()` para insertar con `$executeRaw` (sin `RETURNING`) vía `forUserRaw()`, en vez de abrir una política de self-select que habría ampliado quién puede leer `audit_logs` más allá de lo que pide `plan.md`. `test/agent-reports.e2e-spec.ts`, `admin-crud.e2e-spec.ts` y `followups.e2e-spec.ts` tuvieron que actualizar su helper `assertAudited` para leer con `forUser()` (antes usaban el cliente Prisma crudo, que ahora ve 0 filas sin contexto -- mismo criterio que el resto de las tablas con RLS).
- **`ExportsModule`** (`apps/api/src/exports/`): `GET /exports/reports.csv` streamea por **keyset en lotes de 500** vía `forUserRaw()` (no `DECLARE CURSOR`: el runtime va contra el endpoint *pooled* de Neon, un cursor abierto toda la descarga pinnearía la conexión y las transacciones interactivas de Prisma tienen timeout) con backpressure explícito (`await` en `'drain'` si `res.write()` devuelve `false`). BOM UTF-8, antiinyección de fórmulas (`'` delante de celdas que arrancan con `=+-@`/tab/CR), notas multilínea colapsadas a una fila. `GET /exports/reports.pdf` reusa `ReportsService.summary()`/`findAll(limit: 200)` (cero SQL nuevo) + `pdfmake` con fuentes estándar (Helvetica, sin TTF embebidos). `tenantId` es un filtro opcional que solo tiene efecto para staff (`ReportsService` sumó soporte de `tenantId` en `findAll`/`summary`); para un `client_user` se ignora en vez de dejar que RLS lo neutralice, así el criterio "`?tenantId=` forzado no cambia el archivo" se cumple exacto.
- **`pdfmake` 0.3.11 tiene una API nueva** (no es la `PdfPrinter` clásica de 0.1/0.2): `pdfMake.setFonts(...)` + `pdfMake.createPdf(docDefinition).getBuffer()`. Verificado a mano (`Peña, ¿está María José? Ñoño... ¿¡`) que las fuentes estándar renderizan bien los acentos antes de escribir el resto del bloque. Se agregó `setUrlAccessPolicy(() => false)` + `setLocalAccessPolicy(...)` (denegar todo excepto los 14 nombres de fuente estándar) como defensa en profundidad -- gotcha: pdfmake trata los NOMBRES de fuente como si fueran paths de archivo local, así que una política `() => false` a secas rompe las propias fuentes del documento; hay que permitirlas explícitamente por nombre.
- **Visor de auditoría** (`apps/api/src/audit/audit.service.ts` + `audit.controller.ts`, nuevos, se suman al `AuditModule` ya existente): `GET /admin/audit-logs` (cursor, filtros `userId`/`entityType`/`action`/`from`/`to`) y `GET /admin/audit-logs/filters` (valores de `action`/`entityType` *realmente* presentes en la tabla -- el `AuditAction` de `@callreport/shared` incluye acciones que el backend nunca escribe, son del mock de Fase 3). `@Roles(supervisor, super_admin)`, RLS es el corte real.
- **`main.ts`**: `app.enableCors({ exposedHeaders: ['Content-Disposition'] })` -- sin esto el JS del navegador no puede leer el nombre de archivo que manda `ExportsController` (esa cabecera no es CORS-safelisted por defecto).
- **Móvil**: `expo-file-system` + `expo-sharing` instalados. `src/lib/export-download.ts` usa la API vigente de SDK 57 (`File.downloadFileAsync(url, destination, { headers, idempotent })`, **no** `FileSystem.downloadAsync()` legacy, que tira en runtime) con `Authorization: Bearer` (nunca `?token=` en la query) y un reintento manual en 401 (esta descarga no pasa por `authFetch()`, así que no hereda su refresh automático). Nueva pestaña `(client)/exportar.tsx` (desvío deliberado de "botón + hoja" del `plan.md` a favor de una pestaña propia, siguiendo el prototipo Stitch `ExportarPage.tsx`) con los mismos chips de rango/tipificación que el dashboard, conteo previo (`GET /reports/summary`) y selector CSV/PDF. El dashboard suma un acceso directo "Exportar" que navega con el rango/tipificación activos como params iniciales. La selección inicial desde esos params se resuelve como estado derivado en el render (no `useEffect` + `setState`, ni lectura de `ref` durante el render) -- el linter de React Compiler de este proyecto prohíbe ambos patrones explícitamente.
- **Admin-web**: `src/api/audit.ts` (hooks reales, tipo `AdminAuditLog` propio -- el shape real con `user: {fullName, email}` anidado difiere del `AuditLog` del mock, que tenía `userEmail` plano) + `src/api/exports.ts` (`useDownloadExport`, blob + `<a download>`, nombre leído de `Content-Disposition`). `AuditoriaPage.tsx` conserva toda la UI del prototipo (tabla, pills, fila expandible con el diff) sobre datos reales, suma filtro de entidad + rango de fechas + "Cargar más" (paginado por cursor en vez de la lista completa en memoria del mock). `ClientesPage.tsx` suma export CSV/PDF por fila (cualquier staff) y un bloque "Exportar todas: CSV/PDF" visible solo si `session.user.role === 'super_admin'` (criterio de `plan.md`: "para super_admin").
- **Datos y medición** (D8): `prisma/inflate-reports.ts` (`npm run inflate -- --count=50000` / `-- --clean`) reusa tenants/campañas/tipificaciones/agentes/turnos del seed real -- no crea entidades nuevas, marca cada fila con `external_call_id='bulk:<n>'` para poder limpiarla exacto. `scripts/export-memory-check.ts` mide el criterio de aceptación con un **consumidor lento real** (pausa el socket de respuesta la mayor parte del tiempo vía `node:http`, no un mock) mientras muestrea `process.memoryUsage()` cada 250ms. **Resultado con 50 278 filas** (50 000 sintéticas + ~278 del seed real, vistas por `super_admin`): 12.8 MB descargados en 82.5s, 319 muestras, heap usado promedio primera mitad 55.0 MB vs segunda mitad 50.6 MB (**-8%**, sin crecimiento monótono) -- confirma que el streaming + backpressure de `ExportsService` sostiene memoria estable independientemente del volumen. RSS máximo 414.7 MB (overhead normal de Nest+Prisma+socket.io cargados, no atribuible al CSV en sí -- lo que importa es que el heap no creció). Los datos sintéticos se limpiaron (`--clean`) al terminar; la base de desarrollo quedó como antes de la medición.
- **Tests**: `test/exports.e2e-spec.ts` (7 tests: cabeceras/BOM + conteo contra `/reports/summary`, aislamiento por tenant contrastado contra Prisma, `?tenantId=` forzado sin efecto para `client_user`, filtros `from/to/dispositionId`, `super_admin` ve ambos tenants con columna Empresa, antiinyección + multilínea con un reporte real, PDF válido con `%PDF-`) y `test/audit-viewer.e2e-spec.ts` (5 tests: 403 por rol, paginación por cursor sin huecos/solapamientos comparando páginas chicas contra una grande, acciones de Fases 3/4/6 juntas para un mismo reporte + una mutación de admin independiente, filtro por `userId` contra `count()` de Prisma, inmutabilidad UPDATE/DELETE). Las 10 suites e2e (48 tests) pasan juntas con `--runInBand`, incluida la limpieza de los 50k reportes sintéticos.

### Qué quedó pendiente / deferido

- **El PDF/CSV nunca se probaron a mano en dispositivo físico** -- mismo nivel de verificación que todas las fases anteriores: `tsc --noEmit`/`expo lint` limpios y `expo start --web` bundlea sin errores (1414 módulos) en `apps/mobile`; `tsc -b`/`vite build`/`oxlint` limpios en `admin-web`. Falta la pasada manual: compartir un CSV/PDF real desde el share sheet de iOS/Android y abrirlo.
- `TurnosPage` (admin-web) sigue sobre el `AppStore` mock -- fuera de alcance (necesita el clock-out de supervisor sobre turno ajeno, que no tiene endpoint todavía; anotado desde la Fase 4).
- El diff de `audit_logs` sigue siendo superficial (`before: null`, `after: <valor del body>`, decisión de la Fase 3) -- el visor de esta fase lo muestra tal cual, no se rehizo.
- CORS del API y del gateway de sockets siguen abiertos a cualquier origen (Fase 8), ahora con `exposedHeaders: ['Content-Disposition']` agregado -- si Fase 8 reconstruye la config de CORS desde cero, no perder esa línea.
- Push real (Fase 6) sigue esperando el `eas init` + development build de Fase 8, sin cambios en esta fase.

### Notas operativas

- **`tsx` no emite `emitDecoratorMetadata`** (confirmado con un repro de una línea: `Reflect.getMetadata('design:paramtypes', Cls)` da `undefined` con `tsx`, algo que sí funciona bajo `ts-jest`) -- cualquier script que necesite bootear el `AppModule` real de Nest (inyección de dependencias basada en tipos, como `JwtStrategy(config: ConfigService)`) falla con errores de DI confusos (`Cannot read properties of undefined`) si se corre con `tsx`. `ts-node` tampoco alcanza: sin el `moduleNameMapper` que sí tiene Jest, no resuelve los imports con `.js` del cliente Prisma generado (`generated/prisma/internal/class.js` no existe en disco, solo el `.ts`). La única combinación que funciona fuera de Jest es compilar de verdad y correr el JS ya compilado -- por eso `scripts/export-memory-check.ts` corre vía `npm run export-memory-check` = `nest build && node dist/scripts/export-memory-check.js`, no `tsx scripts/...`. `prisma/seed.ts` e `inflate-reports.ts` sí pueden usar `tsx` sin problema porque no tocan la DI de Nest (Prisma directo).
- **`INSERT ... RETURNING` y RLS** (gotcha nuevo, documentado en `audit.interceptor.ts`): cualquier futura tabla con RLS que Prisma `.create()`/`.update()` necesite escribir desde un rol sin política de SELECT sobre esa fila va a pisar el mismo problema. El patrón de solución (raw `$executeRaw` sin `RETURNING` vía `forUserRaw()`) queda como referencia si vuelve a aparecer.
- `pdfmake` se actualizó a 0.3.11 (la única versión publicada en npm al momento de instalar) -- su API de servidor cambió respecto de las guías viejas basadas en `PdfPrinter`; `pdfmake.d.ts` (declaración ambiental propia, el paquete no trae tipos) documenta el subset usado.
- El streaming del CSV reutiliza `forUserRaw()` (existía desde la Fase 6 para `MetricsModule`) -- la inversión de esa fase se amortizó acá tal como se había anotado en su momento.
- Contraseña de todos los usuarios del seed sigue siendo `Password123!` (sin cambios desde la Fase 1).
